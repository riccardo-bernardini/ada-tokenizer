library project Tokenizer is

   for Source_Dirs use ("src");
   for Object_Dir use "obj/";
   for Library_Name use "tokenizer";
   for Library_Dir use "lib";

   package Ide is
      for Documentation_Dir use "doc/";
   end Ide;

   package Compiler is
      for Switches ("ada") use ("-gnata", "-g", "-gnatwa");
   end Compiler;

end Tokenizer;

